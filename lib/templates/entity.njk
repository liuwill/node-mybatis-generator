package {{ modalPackage }};

{% for name, item in packages %}
import {{ item }};
{% endfor %}

public class {{ tableName }}VO {
{% for name, item in columns %}
    private {{ item.targetType }} {{ item.lowerCamelField }};
{% endfor %}

{% for name, item in columns %}
    public String get{{ item.upperCamelField }}() {
        return {{ item.lowerCamelField }};
    }

    public void set{{ item.upperCamelField }}(String {{ item.lowerCamelField }}) {
        this.{{ item.lowerCamelField }} = {{ item.lowerCamelField }};
    }
{% endfor %}
}
